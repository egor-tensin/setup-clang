v2
--

* Cygwin is no longer supported (and the related parameters
  `cygwin`/`hardlinks` were removed).
* Utilities `clang-{format,tidy}` are installed.
* The LLVM linker `ld.lld` is installed.

v1
--

* Initial release.
